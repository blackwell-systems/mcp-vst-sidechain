# Go
/sidechain
/cmd/sidechain/sidechain
*.test
*.out

# C++ / CMake
cpp/build/
build/
*.o
*.a
*.dylib

# Emitted at runtime by the host (a plugin's enumerated catalog)
plugin-catalog.json

# Editor / OS
.claude/
.DS_Store
INTERNAL-*.md

# Go coverage artifacts (generated by go test -coverprofile / CI)
cover.out
cover.lib.out
